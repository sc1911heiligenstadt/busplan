# Seed enthält echte Personendaten (Trainer, echte Spieltermine) -> niemals committen (nur Cloud)
*.seed.json
seed*.json

# Lokales Claude-Projektgedächtnis, nicht Teil des öffentlichen Repos
CLAUDE.md
